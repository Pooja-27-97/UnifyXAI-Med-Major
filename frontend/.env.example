# Set to "true" once the Flask backend + trained RF/SHAP/LIME are live.
# While false (or unset), the app runs entirely on the built-in mock
# explainability engine at src/data/mockEngine.js.
VITE_USE_LIVE_API=false
VITE_API_BASE_URL=/api
